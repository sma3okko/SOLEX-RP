{
  "name": "solex-site",
  "version": "1.0.0",
  "description": "Site officiel de SOLEX RP",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r . dist/",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "bcrypt": "^5.1.0"
  },
  "engines": {
    "node": "18.x"
  }
}
